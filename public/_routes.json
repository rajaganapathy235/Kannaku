{
  "version": 1,
  "include": [
    "/api/*"
  ],
  "exclude": [
    "/assets/*",
    "/favicon.ico",
    "/favicon.svg",
    "/favicon-*.png",
    "/apple-touch-icon.png",
    "/icon-*.png",
    "/icon-mark.svg",
    "/logo-*.svg",
    "/manifest.json",
    "/robots.txt"
  ]
}
